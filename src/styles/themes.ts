import { buttons } from './buttons';
import { typography } from './typography';
import { avatar } from './user-avatar';

export const theme = {
  margin: '10px',
  typography,
  buttons,
  avatar,
};
